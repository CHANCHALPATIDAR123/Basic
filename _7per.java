package BasicQuestion;

class _7per {
    public static void main(String[] args) {
        double base = 50.0;
        double area = 500.0;
        double height = (2 * area) / base;

        System.out.printf("The height of the triangle is:" + height);
    }
}
